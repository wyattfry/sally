services:
  sally-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: sally-server
    env_file:
      - path: .env
        required: false
      - path: server/.env
        required: false
    ports:
      - "8080:8080"
    restart: unless-stopped
